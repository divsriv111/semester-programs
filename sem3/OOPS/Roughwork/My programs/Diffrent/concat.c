#include<stdio.h>
#include<string.h>
void main()
{
    int i,j,k,l;
    char ch1[20],ch2[20],ch[20];
    printf("Enter 1st String\n");
    gets(ch1);
    printf("\nEnter 2nd String\n");
    gets(ch2);
    for(i=0;ch1[i]!='\0';i++)
    {
    }
    ch1[i];
    for(j=0;ch2[j]!='\0';j++)
    {
    }
    ch2[j];
    ch[i+j];
    for(k=0;k<i;k++)
        ch[k]=ch1[k];
    for(l=0;l<j;l++)
        ch[k+l]=ch2[l];
printf("%d",j);
    printf("New String:- %s",ch);
}

