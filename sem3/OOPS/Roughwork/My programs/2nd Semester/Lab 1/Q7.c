#include<stdio.h>
#include<string.h>
int findw(char[],char[],int,int);
void main();
{
      printf("Enter a String\n");
    char st[200];
    gets(st);
    int l=strlen(st);
    st[l];
    printf("Enter the Word plz");
    char st1[100];
    gets(st1);
    int m=strlen(st1);
    st1[m];
    findw(st,st1,l,m);
}
 int findw(char st[],char st1[],int l,int m);
    {
        int i,j;
        for(i=0;i<m;i++)
        {
            if(st1[i]!=st[i])
            {

            }
